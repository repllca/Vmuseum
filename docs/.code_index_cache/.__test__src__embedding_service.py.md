## ./test/src/embedding_service.py

### 先頭コメント（あれば）
- # ============================================================
- # ローカル埋め込みモデル設定
- # ============================================================
- # 軽量で高速な英語多言語モデル
- # （より高精度にしたい場合は intfloat/multilingual-e5-base に変更）
- # ============================================================
- # テキスト埋め込み関数
- # ============================================================
- # ============================================================
- # 作品群の埋め込み生成
- # ============================================================
-         # 対象フィールドを結合して1つのテキストに

### 主要シンボル（export/def/class/type など）
- def embed_text(text: str) -> np.ndarray:
- def embed_artworks(artworks, fields):

### import/依存（上位だけ）
- import numpy as np
- from sentence_transformers import SentenceTransformer

### 役割（ここを最終的に CODE_INDEX に反映）
- (TBD) このファイルの責務を1行で書く

